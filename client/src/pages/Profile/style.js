import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '85%',
    padding: '3em',
    height: '700px',
  },
}));

export default useStyles;
