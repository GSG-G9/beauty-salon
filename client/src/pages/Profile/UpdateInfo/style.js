import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  editBtn: {
    marginRight: '1em',
  },
  input: {
    marginBottom: '2%',
  },
}));

export default useStyles;
